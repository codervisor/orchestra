{
  "name": "orchestra-ts",
  "private": true,
  "workspaces": [
    "packages/core",
    "packages/cli",
    "packages/algorithms"
  ],
  "scripts": {
    "build": "npm run build --workspaces",
    "test": "npm run test --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  }
}
